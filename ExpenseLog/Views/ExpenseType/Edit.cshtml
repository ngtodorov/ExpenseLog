@model ExpenseLog.Models.ExpenseType

@{
    ViewBag.Title = "Edit";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Edit Expense Type</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.ID)

        <div class="form-group">
            @Html.LabelFor(model => model.Title, htmlAttributes: new { @class = "control-label col-md-2", @style = "color:blue" })
            <div class="col-md-4">
                @Html.EditorFor(model => model.Title, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Title, "", new { @class = "text-danger" })
            </div>
        </div>

        <hr />

        <div class="form-row">
            <div class="col input-group-sm">
                <div class="col-md-offset-2 col-md-10">
                    <!-- Submit Button -->
                    <input type="submit" value="Submit" class="btn btn-outline-primary btn-sm" />
                    <!-- Cancel Button -->
                    @Html.ActionLink("Cancel", "Index", "ExpenseType", null, new { @class = "btn btn-outline-primary btn-sm" })
                    <!-- Delete Button -->
                    @Html.ActionLink("Delete", "Delete", "ExpenseType", new { id = Model.ID }, new {@id="btnDelete", @class = "btn btn-outline-primary btn-sm", onclick = "return confirm('Are you sure you wish to delete this Expense Entity?');" })
                </div>
            </div>
        </div>

    </div>
}



@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    
    @*<script>

        $(document).ready(function () {
            $("#btnDelete").on("click", function (e) {
                e.preventDefault();
                bootbox.confirm("Are you sure you wish to delete this Expense Type?",
                    function (confirmResult) {
                        if (confirmResult) {
                            top.location.href = e.target.href;
                        }
                });
                
            });
        });

    </script>*@
}
