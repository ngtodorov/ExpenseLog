@model ExpenseLog.Models.ExpenseType

@{
    ViewBag.Title = "Delete";
}

<div>
    <h4>Detele Expense Type</h4>
    <hr />

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Title)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Title)
            </dd>

        </dl>

        <hr />
        <div class="form-row">
            <div class="col input-group-sm">
                <div class="col-md-offset-2 col-md-10">
                    <!-- Submit Button -->
                    <!-- <input type="submit" value="Delete" class="btn btn-outline-primary btn-sm" /> -->
                    <!-- Cancel Button -->
                    @Html.ActionLink("Cancel", "Index", "ExpenseType", null, new { @class = "btn btn-outline-primary btn-sm" })
                </div>
                @Html.ValidationMessageFor(model => model.Title, "", new { @class = "text-danger" })
            </div>
        </div>

    }
</div>
